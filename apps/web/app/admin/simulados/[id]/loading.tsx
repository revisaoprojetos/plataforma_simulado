import { AppLoading } from '@/components/admin/app-loading'

/** Animação de carregamento (estilo configurado no tenant) ao abrir um simulado. */
export default function Loading() {
  return <AppLoading />
}
